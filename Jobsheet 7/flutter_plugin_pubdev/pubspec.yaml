name: flutter_plugin_pubdev
description: A Flutter project demonstrating the auto_size_text plugin.

version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  auto_size_text: ^3.0.0 # Tambahkan plugin ini

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
